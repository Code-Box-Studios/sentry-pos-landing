DATABASE_URI=postgresql://cms_user:cms_dev_password@localhost:5433/sentry
PAYLOAD_SECRET=dev-only-secret-change-me

# Where the landing page's "Sign in" button goes. In production this is the owner portal —
# app.sentry.com/login per project-spec §12 — which does not exist yet. Locally it points at the
# POS terminal so the button does something: sign in as maria@kapediaria.ph / sentry-demo.
NEXT_PUBLIC_APP_SIGNIN_URL=http://localhost:3000
